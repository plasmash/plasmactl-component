runtime: plugin
action:
  title: Attach
  description: "Attach a component to a zone"
  arguments:
    - name: component
      title: Component
      description: Component MRN to attach (e.g., interaction.applications.dashboards)
      required: true
    - name: zone
      title: Zone
      description: Zone to attach to (e.g., platform.interaction.interop)
      required: true
  options:
    - name: source
      shorthand: s
      title: Source
      description: Source directory containing layer definitions
      type: string
      default: "."
  result:
    type: object
    properties:
      component:
        type: string
      zone:
        type: string
      attached:
        type: boolean
